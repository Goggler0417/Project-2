# Tagmark v2.27

Integrated fix release.

- Per-tab search state and sticky tab tool areas
- Sticky bookmark selection toolbar
- Shared Tag-tab style tag chips in bookmark/profile/character displays
- Bookmark folders persist independently of schema fields and empty folders remain visible
- Profile tag search with Tag Head restrictions
- Character composite Tag/Profile search using configured Tag Heads
- Field editor settings/delete/order fixes
- Newly added fields are rendered and saved instead of being swallowed by legacy component inference
- Character details default collapsed and toggle from the character name
